# Tests

Planned test layers:

- `UnitTests` for algorithms, billing, and validation.
- `IntegrationTests` for gateway auth, routing, and persistence.
- `LoadTests` for booking concurrency, websocket fan-out, and retry handling.
